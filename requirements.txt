# Direct dependencies — pinned for reproducible deployments.
# To upgrade: bump versions here, then run pip install -r requirements.txt.
streamlit==1.57.0
pdfplumber==0.11.9
pandas==3.0.2
plotly==6.7.0
streamlit-authenticator==0.4.2

# Test
pytest==9.0.3
