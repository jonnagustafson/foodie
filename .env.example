# Authentication (required for self-hosted deployments, optional for local dev)
# If AUTH_USERNAME is not set the app runs without a login gate — only do this locally.
#
# Generate a bcrypt password hash:
#   python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
AUTH_USERNAME=admin
AUTH_NAME=Admin
AUTH_PASSWORD_HASH=$2b$12$replacethiswitharealhash
AUTH_COOKIE_KEY=change-this-to-a-long-random-secret
AUTH_COOKIE_NAME=foodie_auth

# LLM-based item categorization (optional).
# If ANTHROPIC_API_KEY is unset, the app falls back to keyword categorization
# and item names are not canonicalized (e.g. different egg brands stay separate).
ANTHROPIC_API_KEY=
# Model used for categorization. Haiku is cheapest and fast enough for this task.
FOODIE_CATEGORIZER_MODEL=claude-haiku-4-5
